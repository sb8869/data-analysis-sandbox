# Requirements for running both analysis notebooks.
# Datasets are loaded from local CSVs (data/online_news_popularity.csv and
# data/adult_income.csv), which are included in the repository, so
# ucimlrepo is NOT required for normal use.
#
# scikit-learn, xgboost, and lightgbm are PINNED to exact versions because
# the trained models are saved as .joblib artifacts (see api/) and pickled
# objects are sensitive to the library version that created them. If you
# need to use different versions, re-run the "CELL 6-A2" cell at the end of
# each notebook to regenerate the artifacts with your installed versions.

pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
seaborn>=0.12.0
scikit-learn==1.9.0
scipy>=1.9.0
statsmodels>=0.14.0
xgboost==3.2.0
lightgbm==4.6.0
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.0.0

# Only needed if you run fetch_data.py to regenerate the CSVs from UCI:
# ucimlrepo>=0.0.7
