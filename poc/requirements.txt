# Requirements for running the Streamlit POC (poc/app.py).
# Loads the same .joblib artifacts as the API (see ../api/model_artifacts/),
# so the version-pinning notes in ../api/requirements.txt apply here too.

streamlit>=1.30.0
joblib>=1.3.0
pandas>=1.5.0
numpy>=1.23.0
scikit-learn==1.9.0

# Uncomment if the saved classification artifact's model_name is
# "XGBoost (tuned)" or "LightGBM (tuned)":
# xgboost==3.2.0
# lightgbm==4.6.0
